// Log Welcome
console.log('Welcome to Node JS')

// Déclaration de variable
const a = 10;
const b = 25;

// Une fonction qui nous permet de calculer suivant 2 paramètres
function addition (x, y) {
    return x + y
}

// Log du résultat
console.log('Resultat de a + b = ', addition(a, b))
